{
  "name": "@snepalysis/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc --project ./tsconfig.json"
  },
  "devDependencies": {
    "@types/mongoose": "^5.7.8",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "chalk": "^3.0.0",
    "mongoose": "^5.9.7"
  }
}
